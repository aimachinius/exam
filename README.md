"# exam" 
Vào file Dao/DBConnect để chỉnh thông tin cơ sở dữ liệu 
Chạy các đoạn code trong data.sql và insert data.sql để lấy dữ liệu mẫu
chạy lệnh mvn clean package
sau đó chạy lệnh mvn tomcat7:run để chạy server